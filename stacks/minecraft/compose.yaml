services:
  minecraft:
    image: itzg/minecraft-server:latest
    container_name: minecraft
    restart: unless-stopped
    ports:
      - "25565:25565/tcp"
    environment:
      EULA: "TRUE"
      TZ: "Asia/Kolkata"
      MEMORY: "2G"
      # Pin the server so clients do not break when Mojang latest advances.
      VERSION: "26.2"
      ENABLE_ROLLING_LOGS: "true"
      # Require players to authenticate with Mojang/Microsoft accounts.
      ONLINE_MODE: "TRUE"
      # Leave whitelist disabled until allowed player usernames are available.
      # To restrict later, add ENABLE_WHITELIST: "TRUE" and WHITELIST: "user1,user2".
    volumes:
      - minecraft-data:/data
    stdin_open: true
    tty: true

volumes:
  minecraft-data:
    name: minecraft_data
